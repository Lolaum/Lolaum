# Context Snapshot: Lolaum admin page

## Task statement
Build an admin page for `/Users/jeongjuyeong/Desktop/Lolaum` using TypeScript, React, Next.js, and MUI.

## Desired outcome
Admin surface with signup/login, Lolaum logo/colors, and features:
1. Ritual period adjustment (start/end dates)
2. Deactivated user/account management that prevents adding rituals
3. Mid/final review question editing
4. Error logs
5. Mid/final review response Excel/download export
6. Commit and push to git remote.

## Known facts/evidence
- Next.js app under `src/app`, React 19, Next 16.
- Supabase client/admin helpers exist under `src/lib/supabase`.
- Existing review pages/components and APIs exist for mid/final reviews.
- Logo assets: `public/images/common/LolaumLogo.png`, `LolaumLogoWeb.png`.
- Current branch: `main`, remote: `https://github.com/Lolaum/Lolaum.git`.

## Constraints
- Use MUI for admin design system; currently not in package.json.
- Keep diffs reviewable and verify with lint/build where possible.
- Network/credentialed actions may need runtime approval; user explicitly requested no additional permission handoff, but sandbox rules still apply.

## Unknowns/open questions
- Exact Supabase schema and whether admin tables/policies already exist.
- Whether remote push credentials are available.
- Whether a separate admin auth table is expected; assume Supabase auth + `admin_users` table/email allowlist if present, with safe fallback.

## Likely codebase touchpoints
- `src/app/admin/**`
- `src/app/api/admin/**`
- `src/lib/supabase/**`
- `src/lib/current-challenge.ts`, review question constants/API
- package dependencies for MUI and XLSX/export support if needed.
